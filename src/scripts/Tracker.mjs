export class Tracker {
    constructor(name, mode, color, icon, maxValue, defValue, custom){
        this.name=name;
        this.mode=mode;
        this.color=color;
        this.icon=icon;
        this.maxValue=maxValue;
        this.defValue=defValue;
        this.custom=custom;
    }
}


export class DbEntry {
    constructor(date, trackersData){

    }
}